{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "This repository is mirrored to several forges. An unattended merge on one of them makes the others diverge, so updates are merged by hand and pushed to every mirror.",
      "matchUpdateTypes": [
        "major",
        "minor",
        "patch",
        "digest"
      ],
      "automerge": false
    }
  ]
}
